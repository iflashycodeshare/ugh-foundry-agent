{
  "name": "lattice-client",
  "version": "1.0.0",
  "private": true,
  "description": "Runtime connector for sending workspace content to, and receiving governed claims from, Lattice. Tier-agnostic; runs in VS Code terminals, agents, and containers.",
  "license": "MIT",
  "types": "dist/index.d.ts",
  "bin": {
    "lattice": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p ./",
    "test": "vitest run",
    "lattice": "node dist/cli.js"
  },
  "dependencies": {
    "@azure/identity": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
